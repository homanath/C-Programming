//program to clacualte area of arectangle
#include<stdio.h>

int main(){
    int length,breadth;
    printf("Enter length and breadth\n");
    scanf("%d%d",&length,&breadth);
     
    printf("The area of a rectangle is %d",length*breadth);

    return 0;
}
